######## Inventory Service Configuration (Vite) ########
# Use 'http' to call the backend API; 'fake' uses in-memory data.
# NOTE: Vite only exposes env vars prefixed with VITE_ and must be KEY=VALUE.

# VITE_INVENTORY_SERVICE: fake | http
VITE_INVENTORY_SERVICE=http

# VITE_INVENTORY_BASE_URL must include the '/api' prefix for Azure Functions.
# Local development (Azure Functions Core Tools default port):
# VITE_INVENTORY_BASE_URL=http://localhost:7071/api

# Deployed Azure Function App:
VITE_INVENTORY_BASE_URL=https://devices-dev-ao01-func.azurewebsites.net/api

# Optional seed data toggle for the fake service:
# VITE_USE_SEED_DATA=true